{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Chrome",
            "request": "launch",
            "type": "pwa-chrome",
            "url": "http://127.0.0.1:5500",
            "webRoot": "${workspaceFolder}"
        }
    ]
}